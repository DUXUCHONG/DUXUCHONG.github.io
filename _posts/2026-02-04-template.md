---
layout: post
title: "Template"
title_zh: "模板"
title_en: "Template"
date: 2026-02-04
order: 0
categories: [other]
ref: template
---

<!-- AI Generated: bilingual post content (single site, client-side toggle) -->

<div class="content-zh" lang="zh-Hans" markdown="1">

## 写文章（模板）

- **Markdown 格式写作**：用 Markdown 写内容
- **中英文双语格式**：
  - 中文放在 `<div class="content-zh">` 里
  - 英文放在 `<div class="content-en">` 里
- **根据总 post 数填写 `order`**：
  - 新文章想排在最前面：把 `order` 设成一个更大的数字
  - 习惯做法：`order = 当前总 post 数`（或比当前最大 `order` 大 1）
- **文章类型（categories）**：
  - `tech`
  - `other`

## 图片管理（推荐：按文章 ref 分目录）

- **目录约定**：把本篇文章用到的所有图片放到：
  - `assets/img/posts/<ref>/`

例如（本篇文章的 `ref: template` 只是示例；实际建议用全小写 slug）：

```text
assets/img/posts/template/arch.png
assets/img/posts/template/demo-1.webp
```

- **引用方式（兼容 baseurl）**：在 Markdown 里用 `relative_url`：

```md
![架构图]({{ "/assets/img/posts/" | append: page.ref | append: "/arch.png" | relative_url }})
```

</div>

<div class="content-en" lang="en" markdown="1">

## Writing notes (template)

- **Write in Markdown**: use Markdown here
- **Bilingual format (ZH/EN)**:
  - Put Chinese content inside `<div class="content-zh" >`
  - Put English content inside `<div class="content-en" >`
- **Set `order` based on total posts**:
  - Want newer posts first: use a larger `order`
  - Common habit: `order = current total post count` (or max `order` + 1)
- **Post type (`categories`)**: 
  - `tech`
  - `other`

## Images (recommended: per-post folder by `ref`)

- **Folder convention**: put all images for this post under:
  - `assets/img/posts/<ref>/`

Example:

```text
assets/img/posts/how-this-site-is-built/arch.png
assets/img/posts/how-this-site-is-built/demo-1.webp
```

- **How to reference (baseurl-safe)**: use `relative_url` in Markdown:

```md
![Architecture]({{ "/assets/img/posts/" | append: page.ref | append: "/arch.png" | relative_url }})
```


</div>

