## AI Generated: initial Jekyll config for GitHub Pages
title: "DUXUCHONG"
email: "you@example.com"
description: "这里是我的个人主页与博客。"

# User/Org site: keep baseurl empty. Project site: set baseurl: "/repo-name"
baseurl: ""
url: "https://duxuchong.github.io"

theme: minima
minima:
  skin: auto

header_pages:
  - index.md
  - blog.md
  - about.md

show_excerpts: true

plugins:
  - jekyll-feed

# AI Generated: comments (giscus + GitHub Discussions)
# Note: This repo currently has `has_discussions: false` on GitHub.
# Enable Discussions in the repo settings, then set `category` / `category_id`
# (use giscus' generator to obtain the IDs).
comments:
  provider: giscus
  giscus:
    repo: "DUXUCHONG/DUXUCHONG.github.io"
    repo_id: "R_kgDORILIkg"
    # Set these after enabling Discussions and creating a category (e.g. "Comments")
    category: ""
    category_id: ""
    mapping: "pathname"
    strict: "0"
    reactions_enabled: "1"
    emit_metadata: "0"
    input_position: "bottom"
    theme: "preferred_color_scheme"
    lang: "zh-CN"
    loading: "lazy"

author:
  name: "DUXUCHONG"

social_links:
  - platform: github
    user_url: "https://github.com/DUXUCHONG"

